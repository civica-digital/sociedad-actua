= form_for @proyect do |f|
  - if @proyect.errors.any?
    #error_explanation
      %h2= "#{pluralize(@proyect.errors.count, "error")} prohibited this proyect from being saved:"
      %ul
        - @proyect.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
